# Base URL da API (usada por `src/services/http.ts`).
# Desenvolvimento: apontar para o host:porta do `fraghub-api`.
VITE_API_URL=http://127.0.0.1:3001
# Produção com Nginx no mesmo origin (proxy /api → API):
# VITE_API_URL=/api
