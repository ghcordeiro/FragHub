---
phase: 07-v1.0-production
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: 
  - scripts/upgrade.sh
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
  - tests/installer/precheck.bats
  - api/tests/unit/elo.test.ts
  - api/tests/unit/auth.test.ts
  - api/tests/integration/auth.test.ts
autonomous: true
requirements: [UPGRADE-001, CICD-001, TESTS-001]
must_haves:
  truths:
    - "fraghub upgrade command runs with backup and rollback"
    - "CI validates code quality on every push"
    - "Unit tests reach ≥60% coverage on critical modules"
  artifacts:
    - path: scripts/upgrade.sh
      provides: "Upgrade with backup, migration, rollback"
    - path: .github/workflows/ci.yml
      provides: "Parallel CI validation"
    - path: .github/workflows/release.yml
      provides: "Automated GitHub releases"
---

<objective>
Production Release Foundation: upgrade command, CI/CD pipeline, test suite (unit + integration + bash).

Purpose: Enable automated quality gates and safe production upgrades

Output: Upgrade script, working CI/CD, ≥60% coverage in critical modules
</objective>

<execution_context>
@/Users/guilhermecordeiro/www/pessoal/FragHub/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/Users/guilhermecordeiro/www/pessoal/FragHub/.specs/features/upgrade-command/spec.md
@/Users/guilhermecordeiro/www/pessoal/FragHub/.specs/features/ci-cd/spec.md
@/Users/guilhermecordeiro/www/pessoal/FragHub/.specs/features/tests-suite/spec.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement upgrade.sh with backup, migration, rollback</name>
  <files>scripts/upgrade.sh, .shellcheckrc</files>
  <action>
Create `scripts/upgrade.sh` (250+ lines):
1. Read `.fraghub-version`, query GitHub for latest version
2. Require operator to type `yes` for confirmation
3. Backup database: `mysqldump fraghub > /var/backups/fraghub/backup-{timestamp}.sql` with chmod 600
4. Verify backup (file size > 0)
5. Download: `git pull --rebase` or tarball download
6. Apply migrations from `scripts/migrations/` in order (idempotent)
7. Restart services: `systemctl restart fraghub-api nginx` and verify `active (running)`
8. On failure: restore backup, revert files, restart, log error
9. Log all steps to `/var/log/fraghub/upgrade-{timestamp}.log`
10. Update `.fraghub-version` on success
11. Detect active `cs2` process and warn operator
12. Pass ShellCheck (document exceptions in `.shellcheckrc`)
  </action>
  <verify>
    <automated>
shellcheck scripts/upgrade.sh
test -f scripts/upgrade.sh && wc -l scripts/upgrade.sh
grep -E "mysqldump|rollback|schema_migrations" scripts/upgrade.sh | wc -l
    </automated>
  </verify>
  <done>
- ≥250 lines, passes ShellCheck
- All 8 phases implemented: verify, confirm, backup, download, migrate, restart, rollback, log
- Testable with --dry-run flag
  </done>
</task>

<task type="auto">
  <name>Task 2: Create CI/CD workflows (ci.yml, release.yml) with parallel jobs</name>
  <files>.github/workflows/ci.yml, .github/workflows/release.yml, .shellcheckrc, .prettierrc, README.md</files>
  <action>
Create `.github/workflows/ci.yml` (triggered on push/PR):
- ShellCheck job: `shellcheck scripts/**/*.sh`
- TypeScript backend: `tsc --noEmit` in services/fraghub-api/
- TypeScript frontend: `tsc --noEmit` in services/fraghub-web/ (if exists)
- ESLint: `npm run lint` in both
- Prettier: `prettier --check "**/*.{ts,tsx,js,json,md}"`
- Tests: `npm test` in both
- Node.js 20 LTS, ubuntu-22.04, cache node_modules, target <10 min total

Create `.github/workflows/release.yml` (triggered on vX.Y.Z tags):
- Extract version from tag
- Read CHANGELOG.md section for that version
- Create GitHub Release with changelog body
- Fail if release already exists

Update `README.md` with CI status badge for main branch.
Create/update `.shellcheckrc` and `.prettierrc` with project rules.
  </action>
  <verify>
    <automated>
test -f .github/workflows/ci.yml && echo "ci.yml exists"
test -f .github/workflows/release.yml && echo "release.yml exists"
grep "node-version: 20" .github/workflows/ci.yml
grep "ubuntu-22.04" .github/workflows/ci.yml
grep -c "^  [a-z-]*:" .github/workflows/ci.yml
    </automated>
  </verify>
  <done>
- CI and release workflows created
- 6+ parallel jobs in CI
- Node 20 LTS, ubuntu-22.04, caching enabled
- CI badge in README
  </done>
</task>

<task type="auto">
  <name>Task 3: Create unit and integration tests with ≥60% coverage</name>
  <files>api/tests/unit/elo.test.ts, api/tests/unit/auth.test.ts, api/tests/integration/auth.test.ts, tests/installer/precheck.bats</files>
  <action>
Create `api/tests/unit/elo.test.ts` (Jest): ELO rating tests (victory, defeat, draw, high RD, floor ≥100). Min 80 lines.

Create `api/tests/unit/auth.test.ts` (Jest): JWT tests (generation with claims, valid/expired/invalid signature, role required). Min 100 lines.

Create `api/tests/integration/auth.test.ts` (Supertest): POST /auth/login (valid/invalid), rate limiting, GET /players/:id/profile auth. Min 120 lines.

Create `tests/installer/precheck.bats` (bats-core): curl availability, OS/arch validation, minimum resource checks. Min 50 lines.

Update `api/package.json`: Add `npm test` and `npm run test:coverage` scripts.
Ensure coverage ≥60% on critical modules (elo, auth, matchmaking).
  </action>
  <verify>
    <automated>
test -f api/tests/unit/elo.test.ts && wc -l api/tests/unit/elo.test.ts
test -f api/tests/unit/auth.test.ts && wc -l api/tests/unit/auth.test.ts
test -f api/tests/integration/auth.test.ts && wc -l api/tests/integration/auth.test.ts
test -f tests/installer/precheck.bats && wc -l tests/installer/precheck.bats
cd /Users/guilhermecordeiro/www/pessoal/FragHub/services/fraghub-api && npm test 2>&1 | grep -E "PASS|FAIL|coverage"
    </automated>
  </verify>
  <done>
- Unit tests for ELO, auth created (≥100 lines each)
- Integration tests for auth endpoints created (≥120 lines)
- Bash tests with bats-core created (≥50 lines)
- Coverage ≥60% on critical modules
- All tests isolated (no shared state, fresh db per test)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Upgrade operator→script | Operator confirmation input |
| Script→Database | Backup/restore operations |
| CI→GitHub | Automated release creation |
| Test suite→Database | Test isolation via fraghub_test DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-07-01 | S | upgrade.sh input | mitigate | Strict validation: only "yes" accepted |
| T-07-02 | T | backup permissions | mitigate | chmod 600, /var/backups ownership |
| T-07-03 | R | rollback failure | mitigate | Transactional migrations, backup verification |
| T-07-04 | I | CI secret exposure | mitigate | Use ${{ secrets.* }} pattern only |
| T-07-05 | D | test data isolation | mitigate | fraghub_test database, cleanup in hooks |
| T-07-06 | E | unauthorized release | mitigate | Branch protection, CI gating |
</threat_model>

<verification>
- [ ] upgrade.sh passes ShellCheck, all 8 phases implemented
- [ ] CI/CD workflows created with parallel jobs, Node 20, ubuntu-22.04
- [ ] Unit tests created (elo, auth ≥100 lines each)
- [ ] Integration tests created (≥120 lines)
- [ ] Bash tests created (≥50 lines)
- [ ] Coverage ≥60% on critical modules
- [ ] All tests isolated with separate databases
- [ ] CI badge in README.md
- [ ] All files committed
</verification>

<success_criteria>
- [ ] fraghub upgrade command exists and is executable
- [ ] npm test passes in both api/ and web/
- [ ] Code coverage ≥60% on critical modules
- [ ] CI/CD workflows run on every push/PR
- [ ] GitHub Release created automatically on tags
- [ ] ShellCheck passes in all scripts/
- [ ] TypeScript compilation passes (tsc --noEmit)
- [ ] All unit/integration tests pass in <60 seconds
- [ ] Backup files created with correct permissions (600)
- [ ] Rollback logic verified
</success_criteria>

<output>
After completion, create `.planning/phases/07-v1.0-production/07-01-SUMMARY.md`
</output>
