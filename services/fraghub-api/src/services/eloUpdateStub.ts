/** MATCHAPI-REQ-008 — substituir quando existir `elo-system`. */
export function updateElo(matchId: number): void {
  // eslint-disable-next-line no-console -- stub intencional
  console.log(`ELO update pending: matchId=${String(matchId)}`);
}
